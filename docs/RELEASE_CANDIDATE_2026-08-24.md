# Релиз-кандидат от 2026-08-24

Статус: **код проверен локально; не отправлен в remote; не развернут в production**.

## База и границы релиза

- production base: `v0.1.0-pilot.22`, commit `2e8f9c2`;
- production schema: `V44`;
- candidate branch: `codex/livesklad-daily-webhook-protection`;
- кандидат содержит пять продуктовых commit и этот документационный commit поверх production;
- backend runtime-код и Flyway-миграции относительно `pilot.22` не меняются;
- изменение runtime затрагивает SPA; backend-изменения кандидата ограничены тестами.

Перед отправкой нужно повторить `git fetch` с рабочим SSH agent и убедиться, что ветка не отстала
от remote. Локальная ссылка `origin` на момент последней сверки указывала на `2e8f9c2`.

## Что входит

### `b1c0db4 test(ai): promote v21 semantic evaluation`

- актуализирован dataset и evaluator для `v21/schema3`;
- автоматический и ручной семантический прогон: `26/26`, средняя оценка `4.8/5`;
- production default не переключается: остается `v4/schema2`.

### `226df81 feat(plan): refine plan and shift management UX`

- уточнены подписи полного плана, темпа и дневной цели;
- объяснено, почему «Нужно в день» пересчитывается;
- будущие дни отделены от факта;
- улучшены карточки направлений, таблица дней и редактор смен.

### `5fb6dc6 feat(metrics): clarify sales structure and attach benchmarks`

- «Структура продаж» показана иерархически: телефоны являются частью техники, а аксессуары и
  услуги — составными частями дополнительной выручки;
- магазинный benchmark явно обозначен как итог по всем документам магазина;
- продажи вне рейтинга и без сотрудника вынесены в отдельную контекстную колонку;
- цветовая шкала сотрудников сравнивается с магазином и соответствует легенде;
- добавлены backend regression tests расчета категорий и классификации.

### `ac155ed fix(insights): clarify evidence and limited analyses`

- подтверждающие данные находятся рядом с выводом;
- устранены дублирующиеся блоки «Данные»;
- ограниченные/недоступные выводы объясняются без шаблонного выдуманного текста;
- переработана визуальная иерархия раздела «ИИ-разбор».

### `674c1dc docs: record July revenue reconciliation`

- сохранен аудит сверки за июль;
- зафиксированы восемь известных пропущенных возвратов и разница `716 750 ₽`;
- документ не восстанавливает данные сам по себе.

### Документационный commit 2026-08-24

- заменяет устаревшие центральные статусы `V30–V34` на фактический production `V44`;
- добавляет индекс актуальных и исторических документов;
- фиксирует release/rollback, webhook canary и validated recovery процедуры;
- синхронизирует UI, метрики, ИИ, security и env reference с текущим кодом;
- не меняет runtime и не выполняет production operation.

## Проверки кандидата

На текущем дереве до документационного commit выполнены:

- backend: `925` тестов, `0` failures/errors/skipped;
- frontend: `38` test files, `143` tests;
- ESLint, generated-contract check и production build;
- отдельный прогон plan commit: `142` frontend tests и build;
- ИИ: `58` unit tests и `26/26` семантических кейсов;
- Checkstyle, OpenAPI compatibility, supply-chain, security и release-safety checks;
- локальная визуальная проверка `/overview`, `/plan`, `/insights` на desktop/tablet/mobile;
- отдельно просмотрены редактор смен и карта допродаж.

Документационные изменения не являются материальным изменением UI, поэтому повторный
`npm run visual:local` только из-за них не требуется.

## Что намеренно не входит

- восстановление восьми июльских возвратов: это отдельная production-операция;
- локальный `.codex-prod-recovery/recover-july-returns.sh`: временный операторский файл,
  не прошедший требования к постоянному release-артефакту;
- включение `LIVESKLAD_ORDER_RETURN_WEBHOOK_WORKER_ENABLED`: флаг остается `false` до canary;
- переключение production на `v21/schema3`;
- исправление исходных данных LiveSklad, смен МобиСферы и неидентифицированных возвратов без
  подтвержденных внешних ID.

## План безопасного релиза

1. Убедиться, что документационный commit содержит только ожидаемые файлы; временный
   recovery-каталог не добавлять.
2. Повторить целевые проверки и `git diff v0.1.0-pilot.22..HEAD`.
3. Выполнить `git fetch`, проверить divergence и только после этого push.
4. Собрать immutable web image из точного commit; backend image можно оставить текущим, если
   release-процесс поддерживает независимые digest.
5. Проверить `docker compose config` и зафиксировать текущие digest/flags.
6. Развернуть web, проверить health и smoke маршрутов `/overview`, `/plan`, `/insights`.
7. Проверить журнал браузера, HTTP 5xx и ключевые значения на одном известном периоде.
8. При регрессии вернуть предыдущий web digest. Откат БД не требуется, потому что миграций нет.

Production-релиз и восстановление исторических возвратов должны быть двумя разными change records.
