---
doc_schema: 1
doc_type: evidence
status: historical
owner: operations
audience:
  - developer
  - operator
snapshot_date: 2026-09-02
verdict: PASS_WITH_ACCEPTED_LIMIT
verdict_scope: release identity, image provenance, schema, services, queues, public health, synchronized source window and owner-approved rating roster correction
source_of_truth: sanitized production command output, repository release tag and successful CI/release workflow runs
required_reviewers:
  - operations
supersedes: []
superseded_by: null
---

# Production verification: v0.1.0-pilot.30

This is an immutable, sanitized observation from 2026-09-02. It proves only the checks listed
below; it is not a continuously refreshed status page and it is not a restore drill.

## Release identity

| Item | Observed value |
|---|---|
| Release | `v0.1.0-pilot.30` |
| Release commit | `24c1d37affb0d48a610cd3eba33f8f8cc2241a4b` |
| Database schema | `48` |
| Release environment SHA-256 | `5584f363d050530220c1c2e74d9d5bd669a7d73b3eb9dafe00d71c2bc9cf6087` |
| Backend image | `sha256:bff646c1ff7bb35425b5d587cba15399751c6ecd936445c840a9b0c5a4e90a07` |
| Web image | `sha256:89009ae282e593b5004716a594a9e21cc959a995b6ad5d4ba284fff1030a7ed4` |

The annotated Git tag resolved to the stated commit. CI run `33601812218` and release workflow
run `33602958902` completed successfully. Both remote OCI revision labels matched the release
commit before production write. The deployed backend API, backend worker and web containers use
the expected immutable digest references.

## Pre-deploy gates

The latest incremental LiveSklad synchronization completed successfully through
`2026-09-01T22:00:00Z`; no active synchronization or report-backfill job remained. The scheduled
logical backup service completed successfully from 22:53:31 through 22:53:37 UTC on 2026-09-01.
No isolated restore drill was performed, so backup success is not proof of restore correctness,
business reconciliation, RPO or RTO.

The release-specific fail-closed preflight passed. It verified the root-owned release environment,
required secret-file presence, exact database target, immutable image references and digests,
remote OCI revisions, schema compatibility range and production Compose configuration. Critical
deploy-bundle files on the host matched the reviewed repository versions by SHA-256.

## Deployment observation

The deploy ran once. It pulled the reviewed images, stopped the worker and API before Flyway,
validated all 49 packaged migrations and reported schema `app` at version `48` with no migration
necessary. Exact-target ACL repair committed successfully. The API became healthy before the
worker started, and the HTTPS edge started last.

The first frontend probe occurred while the edge container was starting; the bounded smoke retry
then passed. The deploy script reported `Release v0.1.0-pilot.30 deployed successfully`.

## Rating roster correction

After application health was established, one separately reviewed exact-target operation enabled
the newly synchronized seller assignment for rating participation in `МАГАЗИН` only. The active
assignment changed from version 0 to 1 and an `EMPLOYEE_RATING_PARTICIPATION_CHANGED` audit row was
recorded. The corresponding `МобиСфера` assignment remained non-participating and unchanged.

The operation changes eligibility for current UI queries and future weekly-review projections. It
does not rewrite the already persisted review for the completed week `2026-08-24..2026-08-30`.

## Post-deploy checks

- Backend API, backend worker and web were healthy on the expected image digests.
- Public frontend HTTPS, `/livez` and `/readyz` returned HTTP 200 independently after deploy.
- The public `/actuator/prometheus` endpoint returned HTTP 404.
- Flyway history still ended at successful version `48`; no failed migration was observed.
- No active synchronization or report-backfill job was present.
- No structured `ERROR`/`FATAL`, migration-failure or health-timeout marker was found in fresh
  service logs.

## Release behavior and limits

The release aligns weekly-review employee cards with the approved rating roster, limits the shift
selector to active rating participants and streamlines the manager-facing weekly-review layout and
wording for every store. It does not regenerate historical weekly-review snapshots.

Authenticated business screens were covered by automated component, contract and three-viewport
visual checks before release. Production verification covered public health, persisted roster
state and runtime evidence but did not use production user credentials for an authenticated browser
session. No restore, disaster-recovery, alert-delivery or break-glass rehearsal was performed.
