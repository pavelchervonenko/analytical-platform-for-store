---
doc_schema: 1
doc_type: evidence
status: historical
owner: operations
audience:
  - developer
  - operator
snapshot_date: 2026-09-01
verdict: PASS_WITH_ACCEPTED_LIMIT
verdict_scope: release identity, image provenance, schema, services, queues, public health, production bundle and pre-deploy backup observation
source_of_truth: sanitized production command output, repository release tag and successful CI/release workflow runs
required_reviewers:
  - operations
supersedes: []
superseded_by: null
---

# Production verification: v0.1.0-pilot.29

This is an immutable, sanitized observation from 2026-09-01. It proves only the checks listed
below; it is not a continuously refreshed status page and it is not a restore drill.

## Release identity

| Item | Observed value |
|---|---|
| Release | `v0.1.0-pilot.29` |
| Release commit | `0b7d49ce4e79d319089f99c9392ceb6804f92b58` |
| Database schema | `48` |
| Release environment SHA-256 | `be27810bd2e37e19b2378b59b736f26be5d7f8e2f214720587823ddf7a526066` |
| Backend image | `sha256:b17f4d523fb0520ab8ea9e364b0403df5441e5ae11c8383b7fd1fc25024c3247` |
| Web image | `sha256:e00069b40aa0eb0553ed91e8240546501c03f32bc5aabc36ee49c26bb4ec6496` |

The Git tag resolved to the stated commit. CI run `33411876264` and release workflow run
`33413185590` completed successfully. The production preflight verified both immutable image
references, both digest fields and both remote OCI revision labels against the release commit.

## Backup and accepted exception

The pre-deploy logical backup service completed successfully from 22:47:44 through 22:47:48 UTC
on 2026-08-31. No isolated restore drill had been performed. Before production write, the owner
confirmed the exact schema-neutral release plan with the known restore-drill limitation. The
successful backup run is not proof of decryption, restore correctness, business reconciliation,
RPO or RTO.

## Deployment observation

The deploy ran once. It pulled the reviewed images, verified local image provenance, stopped the
worker and API before Flyway and then validated all 49 packaged migrations. Flyway reported schema
`app` at version `48` and applied no migration. The exact-target ACL repair completed for the
runtime, migrator and backup roles before startup continued.

The API became healthy before the worker was started. After deployment, `backend-api`,
`backend-worker` and `web` were all healthy and used the expected digests. The release script
reported `Release v0.1.0-pilot.29 deployed successfully`. The first HTTPS probe occurred while the
edge container was starting; the bounded retry then passed and the deploy completed successfully.

## Post-deploy checks

- Public frontend HTTPS, backend liveness and backend readiness passed independently after deploy.
- Required HTTPS security headers were present and the public actuator endpoint returned HTTP 404.
- Flyway history still ended at successful version `48`; no failed migration was observed.
- No active synchronization or report-backfill job was present.
- All 49 observed sale-return webhook receipts were processed, with no payload mismatch or
  terminal failure.
- No structured `ERROR`/`FATAL`, migration-failure or health-timeout marker was found in the fresh
  service logs. Scanner requests whose URL contained `error.log` were informational access logs,
  not application errors.
- The served production overview bundle contained the scoped sales-structure heading and did not
  contain the removed data-quality issue label.

## Release behavior and limits

The release makes sales structure follow the same `SELLERS`/`STORE` scope as the top overview
metrics, keeps sellers as the default scope, replaces separator dots in the sales-structure and
monthly-plan headings, and removes the open data-quality issue indicator from the freshness banner.
The quality counter remains available to internal API and quality-page consumers.

The authenticated overview screen was covered by automated component, contract and three-viewport
visual checks before release. Production verification checked the served bundle and public health,
but did not use production user credentials to exercise the authenticated business screen.

No historical return reprocessing or data backfill was run as part of deployment. No restore,
disaster-recovery, alert-delivery or break-glass rehearsal was performed. Monitoring rules present
in the repository are not evidence that external alert routing is connected. A new production
observation supersedes the dynamic values here; it must not edit this record.
