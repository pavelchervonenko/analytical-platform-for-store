---
doc_schema: 1
doc_type: evidence
status: historical
owner: operations
audience:
  - developer
  - operator
snapshot_date: 2026-08-30
verdict: PASS_WITH_LIMITS
verdict_scope: release identity, schema, images, services, weekly-review flags and pre-deploy backup observation
source_of_truth: sanitized production command output supplied by the operator and repository release tag
required_reviewers:
  - operations
supersedes: []
superseded_by: null
---

# Production verification: v0.1.0-pilot.27

This is an immutable, sanitized observation from 2026-08-30. It proves only the checks listed
below; it is not a continuously refreshed status page and it is not a restore drill.

## Release identity

| Item | Observed value |
|---|---|
| Release | `v0.1.0-pilot.27` |
| Release commit | `ea90ec81c3c33729e86d515e937bd9d82c39e636` |
| Database schema | `48` |
| Backend image | `sha256:ca73220219b27c1aa0b738dedfb19b4d6c3caf2086bb5f146ce214b6769c6feb` |
| Web image | `sha256:1953f68c53755a0390ffe79233ce6fae7b7deea21c96fa3a3e51b050766b595c` |

The annotated Git tag resolves to the stated commit in the repository. Release preflight passed
before migration. After deployment, `backend-api`, `backend-worker` and `web` were all reported as
`healthy` and used the image digests above.

## Weekly-review flags

The operator's allowlisted environment check reported:

```dotenv
WEEKLY_REVIEW_ENABLED=true
WEEKLY_REVIEW_SNAPSHOT_PLANNER_ENABLED=false
WEEKLY_REVIEW_AI_ENABLED=true
WEEKLY_REVIEW_AI_PLANNER_ENABLED=false
WEEKLY_REVIEW_AI_WORKER_ENABLED=true
```

This configuration permits the new read path and manually queued AI work. It does not enable
automatic snapshot or AI planning.

## Pre-deploy backup observation

Before the `pilot.27` deployment, `store-analytics-backup.timer` and the public health monitor were
active. The latest shown backup
service run completed successfully on 2026-08-29 at 22:47 UTC and reported an uploaded, verified
encrypted logical dump. This proves successful script execution and object-size verification only.
It does not prove decryption, isolated restore, business reconciliation, RPO or RTO.

## Public edge check

On 2026-08-30 the public `/livez` and `/readyz` endpoints returned HTTP 200 with `UP`. HSTS,
`X-Content-Type-Options: nosniff` and `X-Frame-Options: DENY` were present. Public
`/actuator/info` returned 404 as intended by the edge configuration.

## Weekly-review canary

The first exact `weekly-interpretation-v25/schema4` canary for `МобиСфера` completed in one attempt,
passed structural and semantic validation and changed no backend-owned fields. Full canary evidence
remains in [the v25 rollout record](../../../canaries/2026/08/weekly-review-v25-rollout.md).

This single-store canary does not authorize automatic mass generation. Both planners remained
disabled at the end of the observation.

## Explicit limits

- LiveSklad flags and webhook queues were not re-verified in this evidence snapshot.
- Queue counts observed before deployment are intentionally excluded from the post-deploy verdict.
- No restore, disaster-recovery, alert-delivery or break-glass rehearsal was performed.
- Monitoring rules present in the repository are not evidence that Prometheus/Alertmanager is
  connected in production.
- A new production observation supersedes the dynamic values here; it must not edit this record.
