---
doc_schema: 1
doc_type: evidence
status: historical
owner: operations
audience:
  - developer
  - operator
snapshot_date: 2026-08-31
verdict: PASS_WITH_ACCEPTED_LIMIT
verdict_scope: release identity, image provenance, schema, services, queues, public health and pre-deploy backup observation
source_of_truth: sanitized production command output, repository release tag and successful CI/release workflow runs
required_reviewers:
  - operations
supersedes: []
superseded_by: null
---

# Production verification: v0.1.0-pilot.28

This is an immutable, sanitized observation from 2026-08-31. It proves only the checks listed
below; it is not a continuously refreshed status page and it is not a restore drill.

## Release identity

| Item | Observed value |
|---|---|
| Release | `v0.1.0-pilot.28` |
| Release commit | `f130e0931f746d822e387292149bd57de4d5493e` |
| Database schema | `48` |
| Release environment SHA-256 | `eb4d251e6ef66c1f4e0d4f65bcaf52991e2ac85b61b161a9776cd362833e682d` |
| Backend image | `sha256:275db7f435dfeeb7db499200ee4c63cc14bce73b9e2772173185e10f3449a89f` |
| Web image | `sha256:7e362208a01d98656300662c714e796e0a5197292c865356b8238c5532f2b82d` |

The Git tag resolved to the stated commit. CI run `33387223376` and release workflow run
`33388497978` completed successfully. The production preflight verified both immutable image
references, both digest fields and both remote OCI revision labels against the release commit.

## Backup and accepted exception

The pre-deploy logical backup service completed successfully from 12:10:57 through 12:11:03 UTC.
No isolated restore drill had been performed. Before production write, the owner explicitly
accepted that limitation for this schema-neutral release. The successful backup run is not proof
of decryption, restore correctness, business reconciliation, RPO or RTO.

## Deployment observation

The deploy ran once. It pulled the reviewed images, verified local image provenance, stopped the
worker and API before Flyway and then validated all 49 packaged migrations. Flyway reported schema
`app` at version `48` and applied no migration. The exact-target ACL repair completed for the
runtime, migrator and backup roles before startup continued.

The API became healthy before the worker was started. After deployment, `backend-api`,
`backend-worker` and `web` were all healthy and used the expected digests. The release script
reported `Release v0.1.0-pilot.28 deployed successfully`.

## Post-deploy checks

- Public frontend HTTPS, backend liveness and backend readiness passed.
- The intentionally non-public actuator endpoint returned HTTP 404.
- Flyway history still ended at successful version `48`; no failed migration was observed.
- No active synchronization or report-backfill job was present.
- All 44 observed sale-return webhook receipts were processed, with no ready item, payload
  mismatch or terminal failure.
- No `ERROR`, `FATAL`, migration-failure or health-timeout marker was found in the fresh service
  logs at 12:41 UTC.

## Release behavior and limits

The release contains the overview metric scope switch with sellers as the default, the redesigned
weekly AI review and original-sale employee attribution for newly synchronized or deliberately
reprocessed returns. These authenticated business screens were covered by pre-release automated
and local visual checks, but were not exercised with production user credentials during this
verification.

No historical return reprocessing or data backfill was run as part of deployment. Returns whose
original sale is not loaded remain unresolved until the missing source period is loaded and a
separately approved reprocessing operation is performed.

No restore, disaster-recovery, alert-delivery or break-glass rehearsal was performed. Monitoring
rules present in the repository are not evidence that external alert routing is connected. A new
production observation supersedes the dynamic values here; it must not edit this record.
