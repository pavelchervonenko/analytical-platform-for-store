# Copy this file to .env.visual.local and keep real credentials local.
VISUAL_EMAIL=replace-with-local-email
VISUAL_PASSWORD=replace-with-local-password
VISUAL_ROUTES=/insights

# Optional when the Vite server is already running locally.
# VISUAL_BASE_URL=http://127.0.0.1:5173
